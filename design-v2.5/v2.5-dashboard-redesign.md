# v2.5 ダッシュボード刷新仕様

> **バージョン**: 2.5  
> **作成日**: 2026-05-19  
> **正式保管先**: `C:\探究プロジェクト\tankyu-rpg\design\v2.5\v2.5-dashboard-redesign.md`  
> **対象ファイル**: `app/frontend/src/pages/DashboardPage.tsx`  
> **目的**: 現状の「枠と文字だけのスカスカ UI」を、事務所マップを中心とした没入感のあるエントランス画面に刷新する。

---

## 1. 現状の問題点

- メインコンテンツが「コルクボード風グリッド + 章カード + レーダーチャート + 称号」の4ブロック並列で、視覚的な主役がない
- マップが CSS の付箋風ラベルだけで、絵としての訴求力がない
- 「前回の続きから」が章カード内のサブ要素になっている
- レーダーチャートが画面の右1/3を占有

---

## 2. 刷新方針

**ダッシュボード = 「事務所の入口に立った瞬間」**

主役は **事務所マップビジュアル** (`/assets/dashboard/bg_office_map.png`)。マップ上に3つのクリッカブルホットスポット（入口・板・調査室）を置き、サイドのレーダー/称号は「机の上の手帳」として小さく配置する。

---

## 3. レイアウト（PC / `md:` 以上）

```
┌─────────────────────────────────────────────────────────────────┐
│ ヘッダー: ヨアケ探偵社 / [ジャーナル] [プロフィール] [ログアウト]  │
├─────────────────────────────────────────────────────────────────┤
│   ┌─────────────────────────────────────┐  ┌──────────────┐   │
│   │      事務所マップ ビジュアル          │  │ 進捗ミニカード │   │
│   │      bg_office_map.png              │  │ コマ X/8     │   │
│   │   ◉ 入口    ◉ 板    ◉ 調査室         │  └──────────────┘   │
│   │                                     │  ┌──────────────┐   │
│   │   [ 前回の続きから → ] (大ボタン)    │  │ 探究力(小)   │   │
│   │   [ 最初から始める ] (リンク)        │  └──────────────┘   │
│   └─────────────────────────────────────┘  ┌──────────────┐   │
│                                             │ 称号(小)     │   │
│                                             └──────────────┘   │
│   [dev] 進捗をリセット                                            │
└─────────────────────────────────────────────────────────────────┘
```

- マップエリア: 12カラム中9カラム / サイドミニカード群: 3カラム

---

## 4. レイアウト（モバイル / `< md`）

縦積み: マップ → 大ボタン → 進捗 → 探究力 → 称号

---

## 5. 事務所マップ（メインビジュアル）

### 5-1. 画像
- ファイル: `/assets/dashboard/bg_office_map.png`
- 推奨サイズ: 1280×720（16:9）
- スタイル: yoake パレット、温かみのある俯瞰イラスト or 立体パース

### 5-2. クリッカブルホットスポット

| ID | 位置（マップ画像比） | 機能 | 表記 |
|----|--------------------|------|------|
| `entrance` | 左下 (12%, 78%) | プレイ開始 / 続きから | 「入口」 |
| `board` | 中央右 (68%, 55%) | ジャーナル | 「板」 |
| `research` | 右下 (80%, 80%) | 証拠ボード（モーダル） | 「調査室」 |

```tsx
<button
  className="absolute group"
  style={{ left: '12%', top: '78%', width: '14%', height: '14%' }}
  onClick={handleEntranceClick}
  aria-label="入口（プレイ）"
>
  <span className="absolute inset-0 rounded-full bg-yoake-accent/0 group-hover:bg-yoake-accent/20 transition-colors" />
  <span className="absolute inset-0 ring-2 ring-yoake-accent/0 group-hover:ring-yoake-accent/60 rounded-full animate-pulse-soft" />
  <span className="absolute -bottom-6 left-1/2 -translate-x-1/2 text-yoake-ink text-xs font-serif whitespace-nowrap bg-yoake-bg/90 px-2 py-0.5 opacity-0 group-hover:opacity-100 transition-opacity">
    入口
  </span>
</button>
```

### 5-3. 「前回の続きから」ボタン
- マップ下部に大きく配置（高さ 64px、`bg-yoake-accent`）
- 進捗あり時のみ表示。進捗なし時は `物語をはじめる →`

---

## 6. サイドミニカード仕様

### 6-1. 進捗ミニ
```
第1章 SNSの向こう側
─────────────
コマ 2 / 8
▓▓▓▓░░░░ 25%
```

### 6-2. 探究力ミニ（レーダーチャート廃止）
横棒4本に置換：
```
問い力   ▓▓▓░░  3
探索力   ▓▓▓▓▓  5
繋ぐ力   ▓░░░░  1
伝える力 ▓░░░░  1
```
「もっと見る」リンクでプロフィールへ。

### 6-3. 称号ミニ
achievements が空ならカード自体を非表示。

---

## 7. アニメーション

- ロード時: マップ `fadeIn 800ms`、ミニカード `slideUp 600ms stagger 120ms`
- ホットスポット常時 `pulse-soft 3s ease-in-out infinite`、hover で `1.5s`
- 「前回の続きから」hover で `translateX(4px)` + `box-shadow` 強化

---

## 8. レスポンシブ

| ブレークポイント | レイアウト |
|----------------|----------|
| `< 640px` | 縦積み |
| `640〜1024px` | 縦積み（フォントUP） |
| `≥ 1024px` | 9:3 横並び |

ホットスポット座標は %指定でリサイズ追従。

---

## 9. 実装変更点（DashboardPage.tsx）

1. 既存のコルクボード風 div（96〜144行）を削除
2. `<div className="relative aspect-video">` に `<img src="/assets/dashboard/bg_office_map.png" />` を絶対配置
3. ホットスポット3つを絶対配置で重ねる
4. 章カード本体は廃止し、「前回の続きから」「最初から」だけマップ下に大きく配置
5. recharts レーダーを横棒4本に置換
6. 称号は achievements が空ならカードごと非表示

---

## 10. 完了基準

- [ ] `bg_office_map.png` がマップ枠にフィット
- [ ] 入口/板/調査室の3ホットスポットがクリックで遷移
- [ ] 「前回の続きから」が画面で最も目立つ
- [ ] モバイル（375px）で崩れない
- [ ] レーダー / 称号が脇役化

---

*作成: Verdent AI × ヨアケ探偵社プロジェクト / 2026-05-19*
